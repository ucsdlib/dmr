general:
  artifacts:
    - "tmp"

machine:
  node:
    version: 6.1.0
  ruby:
    version:
      2.2.3        
  environment:
    CIRCLE_ENV: test
    APPS_DMR_STREAMING_KEY: 1234567891234567
    RAILS_ENV: test
    CIRCLE_TOKEN: 639be04c64a2560d99ebac4f886cd9a5d253518f
    ACCESSLINT_GITHUB_TOKEN: ec514b3d574d8de9c840eabb5d65223d42b893b5
    
dependencies:
  override:
    - bundle check --path=vendor/bundle || bundle install --path=vendor/bundle --jobs=4 --retry=3
    - npm install -g accesslint-cli
    - gem install accesslint-ci
    
database:
  override:
    - bundle exec rake db:migrate

test:
  post:
    - bundle exec rails server -d -p 3000
    - accesslint-ci scan http://localhost:3000