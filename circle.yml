machine:
  ruby:
    version:
      2.2.3        
  environment:
    CIRCLE_ENV: test
    DMR_STREAMING_KEY: 1234567891234567

dependencies:
  pre:
    - gem install bundler --pre

  override:
    - bundle install

database:
  override:
    - bundle exec rake db:migrate RAILS_ENV=test

test:
  post:
    - bundle exec rake
